# i18n Coverage Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Every user-facing string in the wishlist/wish create-edit forms and the components built during the 2026-08-24 mock-cleanup session (gift pot, comments, account management, public profile) responds to the `/en` vs `/fr` locale switcher, instead of a mix of hardcoded French and hardcoded English regardless of locale.

**Architecture:** next-intl is already fully wired (locale-prefixed routing, message loading, `useTranslations()` used correctly in ~15 other components). For the pre-existing forms, translation keys matching the current hardcoded French text **already exist, unused**, in both `en.json` and `fr.json` — this is a wiring fix, not new infrastructure. For components built this session, no keys exist yet, so those tasks add them. Work proceeds file-by-file: import `useTranslations`, replace each literal string with `t('key')`, delete now-orphaned keys, add net-new keys with real copy in both locale files in the same task.

**Tech Stack:** next-intl (already a dependency). No new libraries.

**Spec:** None — this plan's scope was derived directly from a codebase audit (see file-by-file breakdown in each task; there is no separate spec document to cross-reference).

## Global Constraints

- No test runner exists in this project (confirmed in `CLAUDE.md`). Every task's verification step is `npx tsc --noEmit -p .` (must be silent) plus a manual check: run `pnpm dev` (or `npm run dev`), visit the affected page at both `/en/...` and `/fr/...`, and confirm the string switches language.
- A key must never land in one locale file without its pair — every task that touches `en.json` touches `fr.json` in the same step.
- Match the existing project convention exactly: `const t = useTranslations('namespace')` then `t('key')`; dynamic values use next-intl's `{param}` interpolation and ICU plural syntax, the same syntax already used in e.g. `wishlist.items`: `"{count, plural, one {# item} other {# items}}"`.
- `src/entities/wishlist/lib/validateWishlistForm.ts` and the inline `validateForm` closures in `AddWish/model.ts` / `EditWish/model.ts` are plain functions/hooks, not components — `validateWishlistForm` takes a `t` function as a new parameter (passed by its two callers, which are hooks and can call `useTranslations` themselves); the wish-form validators call `useTranslations` directly since they're already hooks.
- Server-side validation error messages returned by API routes (e.g. `"Name is required"` from `/api/wish`) are English literals in JSON error responses and are **out of scope** for this plan — only client-rendered UI strings are addressed here. Flag this as a follow-up if it matters later.
- Files referenced below use their current (post mock-cleanup-session) content — read the file before editing in case something shifted.

---

## File Structure

No new files are created except two small validation-message additions to the two locale JSON files (existing files, new keys). Every task modifies existing `.tsx`/`.ts` files to consume `useTranslations()` instead of literals, plus the two locale JSON files where new keys are needed.

---

### Task 1: Wire `WishlistForm.tsx` to existing keys, retire dead settings keys

**Files:**
- Modify: `src/entities/wishlist/ui/WishlistForm.tsx`
- Modify: `src/shared/i18n/messages/en.json` (namespace `wishlistForm`)
- Modify: `src/shared/i18n/messages/fr.json` (namespace `wishlistForm`)

**Interfaces:**
- Consumes: `useTranslations` from `next-intl` (already used elsewhere, e.g. `src/features/LanguageSwitcher`).
- Produces: nothing new consumed by later tasks.

- [ ] **Step 1: Delete the two orphaned keys**

`allowComments`, `allowCommentsDesc`, `notifyOnPurchase`, `notifyOnPurchaseDesc` under `wishlistForm` in both JSON files are dead — the `allowComments`/`notifyOnPurchase` settings were removed from the app earlier this session (comments are now always on; purchase notifications were dropped). Delete these four keys from `wishlistForm` in **both** `en.json` and `fr.json`.

- [ ] **Step 2: Add one new key for the "comments always on" preview badge**

`WishlistForm.tsx` line 165 currently hardcodes `💬 Comments always on` in the settings preview. Add to `wishlistForm` in `en.json`:

```json
"commentsAlwaysOn": "💬 Comments always on"
```

and to `fr.json`:

```json
"commentsAlwaysOn": "💬 Commentaires toujours activés"
```

- [ ] **Step 3: Wire the component**

In `src/entities/wishlist/ui/WishlistForm.tsx`, add the import and hook call, then replace every literal per this table (all keys already exist in `wishlistForm` except `commentsAlwaysOn` added above):

```tsx
import { useTranslations } from "next-intl"
```

```tsx
export const WishlistForm = ({
     formData,
     errors,
     isSubmitting,
     handleInputChange,
     handleSubmit,
     onCancel,
 }: TWishlistForm) => {
    const t = useTranslations('wishlistForm')
```

| Line(s) | Current literal | Replace with |
|---|---|---|
| 24 | `Nom de la Wishlist *` | `{t('nameLabel')}` |
| 32 | `placeholder="Entrez le nom de la wishlist"` | `placeholder={t('namePlaceholder')}` |
| 40 | `Description *` | `{t('descriptionLabel')}` |
| 47 | `placeholder="Décrivez votre wishlist..."` | `placeholder={t('descriptionPlaceholder')}` |
| 56 | `Date de l'événement *` | `{t('eventDateLabel')}` |
| 70 | `Confidentialité` | `{t('privacyLabel')}` |
| 81 | `Privée` | `{t('privateLabel')}` |
| 82 | `Seulement vous pouvez voir cette wishlist` | `{t('privateDesc')}` |
| 94 | `Publique` | `{t('publicLabel')}` |
| 95 | `Toute personne avec le lien peut voir` | `{t('publicDesc')}` |
| 104 | `URL de l'image de couverture` | `{t('coverImageLabel')}` |
| 119 | `Paramètres de la Wishlist` | `{t('settingsLabel')}` |
| 129 | `Autoriser les suggestions` | `{t('allowSuggestions')}` |
| 130 | `Permettre aux autres de suggérer des articles` | `{t('allowSuggestionsDesc')}` |
| 139 | `Aperçu` | `{t('preview')}` |
| 144 | `alt="Aperçu de la couverture"` | `alt={t('preview')}` |
| 152 | `formData.name || "Nom de la Wishlist"` | `` formData.name || t('previewName') `` |
| 157 | `formData.isPublic ? "Publique" : "Privée"` | `formData.isPublic ? t('publicLabel') : t('privateLabel')` |
| 162 | `formData.description || "Description de la wishlist"` | `` formData.description || t('previewDescription') `` |
| 165 | `💬 Comments always on` | `{t('commentsAlwaysOn')}` |
| 167 | `💡 Suggestions` — this one has **no matching key**; add `"suggestionsBadge": "💡 Suggestions"` / `"💡 Suggestions"` (identical, emoji-led) to both JSON files under `wishlistForm`, then use `{t('suggestionsBadge')}` | |
| 181 | `Annuler` | `{t('cancel')}` |
| 188 | `isSubmitting ? "Enregistrement..." : "Enregistrer"` | `isSubmitting ? t('saving') : t('save')` |

- [ ] **Step 4: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: start the dev server, log in, open "Create New Wishlist" at `/en/wishlists`, confirm all labels are English; switch to `/fr/wishlists` and reopen — confirm every label/placeholder/button is now French (previously it was always French regardless of locale).

- [ ] **Step 5: Commit**

```bash
git add src/entities/wishlist/ui/WishlistForm.tsx src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): wire WishlistForm to translations, drop dead setting keys"
```

---

### Task 2: Wire `WishForm.tsx` to existing keys

**Files:**
- Modify: `src/entities/wish/ui/WishForm.tsx`

**Interfaces:**
- Consumes: `useTranslations('wishForm')` — all keys already exist in both locale files (see `en.json:205-224` / `fr.json:205-224`), no JSON changes needed for this task.

- [ ] **Step 1: Read the file first**

`WishForm.tsx` wasn't fully read during planning — its labels weren't grepped exhaustively. Before editing, read `src/entities/wish/ui/WishForm.tsx` in full and match every literal string against the existing `wishForm` namespace keys: `nameLabel, priorityLabel, priorityLow, priorityMedium, priorityHigh, descriptionLabel, priceLabel, imageUrlLabel, purchaseUrlLabel, notesLabel, showToOwner, preview, previewName, previewDescription, noImage, cancel, adding, addItem`. The French values in `fr.json` (e.g. `"Nom *"`, `"Priorité"`, `"Faible"`, `"Moyenne"`, `"Haute"`, `"Prix *"`, `"URL de l'image"`, `"URL d'achat"`, `"Montrer au propriétaire"`, `"Pas d'image"`, `"Ajout..."`, `"Ajouter"`) tell you exactly which hardcoded string maps to which key — grep the component for each French string to find its exact location.

- [ ] **Step 2: Wire the component**

Add `const t = useTranslations('wishForm')` (with the `useTranslations` import from `next-intl`) and replace each literal found in Step 1 with the matching `t('key')` call, following the same pattern as Task 1 Step 3 (label text, placeholder props, and the submit-button ternary all follow the identical shape).

If any literal in the file has no matching key (e.g. a placeholder string that Task 2's audit didn't anticipate), add it to `wishForm` in both `en.json` and `fr.json` with real English and French copy before wiring it — do not leave it hardcoded.

- [ ] **Step 3: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: open "Add Item" on any wishlist you own at `/en/wishlist/[id]`, confirm English; switch to `/fr/wishlist/[id]`, reopen "Add Item" (will read "Ajouter un article" or similar once Task 3 also lands — for this task alone, just confirm the form's own labels are French), confirm every label/placeholder is French.

- [ ] **Step 4: Commit**

```bash
git add src/entities/wish/ui/WishForm.tsx
git commit -m "fix(i18n): wire WishForm to translations"
```

---

### Task 3: Wire the five modal titles

**Files:**
- Modify: `src/features/CreateWishlist/ui/CreateWishlistModal.tsx`
- Modify: `src/features/UpdateWishlist/ui/UpdateWishlistModal.tsx`
- Modify: `src/features/AddWish/ui/AddWishModal.tsx`
- Modify: `src/features/EditWish/ui/EditWishModal.tsx`
- Modify: `src/features/ProposeWish/ui/ProposeWishModal.tsx`

**Interfaces:**
- Consumes: `useTranslations('createWishlistModal')` / `'updateWishlistModal'` / `'addWishModal'` / `'editWishModal'` / `'proposeWishModal'` — each namespace has exactly one key, `title`, already present in both locale files (`en.json:225-236`, `fr.json:225-236`).

- [ ] **Step 1: `CreateWishlistModal.tsx`**

Find the `<Modal ... title="Créer une Wishlist" ...>` prop (confirmed at the point this modal is rendered). Add:

```tsx
import { useTranslations } from 'next-intl'
```

Inside the component body, before the `return`:

```tsx
const t = useTranslations('createWishlistModal')
```

Replace the title prop:

```tsx
title={t('title')}
```

- [ ] **Step 2: `UpdateWishlistModal.tsx`**

Same pattern — namespace `updateWishlistModal`, current literal is `"Modifier la Wishlist"`.

- [ ] **Step 3: `AddWishModal.tsx`**

Same pattern — namespace `addWishModal`, current literal is `"Ajouter un souhait"`.

- [ ] **Step 4: `EditWishModal.tsx`**

Same pattern — namespace `editWishModal`. Read the file first to find the exact current literal (not confirmed during planning) and confirm it matches the intent of `"Edit wish"` / `"Modifier le souhait"`; if the on-screen title differs from what `editWishModal.title` says, update the JSON value to match the actual UI copy rather than silently changing the visible title.

- [ ] **Step 5: `ProposeWishModal.tsx`**

Same pattern — namespace `proposeWishModal`. Read the file first (not confirmed during planning) for the exact current literal.

- [ ] **Step 6: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: at `/fr/wishlists`, open each of the five modals (create wishlist, edit wishlist via a card's Edit button, add item, edit item, suggest a wish as a non-owner) and confirm each title is in French; repeat at `/en/...` and confirm English.

- [ ] **Step 7: Commit**

```bash
git add src/features/CreateWishlist/ui/CreateWishlistModal.tsx src/features/UpdateWishlist/ui/UpdateWishlistModal.tsx src/features/AddWish/ui/AddWishModal.tsx src/features/EditWish/ui/EditWishModal.tsx src/features/ProposeWish/ui/ProposeWishModal.tsx
git commit -m "fix(i18n): wire modal titles to translations"
```

---

### Task 4: Translate wishlist form validation errors

**Files:**
- Modify: `src/entities/wishlist/lib/validateWishlistForm.ts`
- Modify: `src/features/CreateWishlist/model.ts`
- Modify: `src/features/UpdateWishlist/model.ts`
- Modify: `src/shared/i18n/messages/en.json` (new namespace `wishlistForm.errors`)
- Modify: `src/shared/i18n/messages/fr.json` (new namespace `wishlistForm.errors`)

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: `validateWishlistForm(formData, t)` — note the new second parameter — consumed by both model hooks below. Any other future caller of `validateWishlistForm` must be updated to pass `t` too (currently only these two call it, per a repo-wide grep).

- [ ] **Step 1: Add the error-message keys**

Add to `en.json`, nested under `wishlistForm` as a new `errors` object:

```json
"errors": {
  "nameRequired": "Wishlist name is required",
  "nameTooShort": "Name must be at least 3 characters",
  "descriptionRequired": "Description is required",
  "descriptionTooShort": "Description must be at least 10 characters",
  "invalidImageUrl": "Invalid image URL",
  "eventDateRequired": "Event date is required",
  "invalidDate": "Invalid date"
}
```

Add to `fr.json`, same shape:

```json
"errors": {
  "nameRequired": "Le nom de la wishlist est requis",
  "nameTooShort": "Le nom doit contenir au moins 3 caractères",
  "descriptionRequired": "La description est requise",
  "descriptionTooShort": "La description doit contenir au moins 10 caractères",
  "invalidImageUrl": "URL d'image invalide",
  "eventDateRequired": "La date de l'événement est requise",
  "invalidDate": "Date invalide"
}
```

- [ ] **Step 2: Update `validateWishlistForm.ts` to accept a translate function**

Replace the full file:

```typescript
import type {TWishlistValidationErrors} from '@/entities/wishlist';

import {isValidUrl} from '@/shared/lib/isValidUrl';

type TValidateWishlistFormData = {
    name: string
    description: string
    coverImage?: string
    eventDate: string
}

export const validateWishlistForm = (
    formData: TValidateWishlistFormData,
    t: (key: string) => string,
): {
    errorList: TWishlistValidationErrors,
    hasError: boolean
} => {
    const errorList: TWishlistValidationErrors = {}

    if (!formData.name.trim()) {
        errorList.name = t('errors.nameRequired')
    } else if (formData.name.trim().length < 3) {
        errorList.name = t('errors.nameTooShort')
    }

    if (!formData.description.trim()) {
        errorList.description = t('errors.descriptionRequired')
    } else if (formData.description.trim().length < 10) {
        errorList.description = t('errors.descriptionTooShort')
    }

    if (formData.coverImage && !isValidUrl(formData.coverImage)) {
        errorList.coverImage = t('errors.invalidImageUrl')
    }

    if (!formData.eventDate) {
        errorList.eventDate = t('errors.eventDateRequired')
    } else if (isNaN(new Date(formData.eventDate).getTime())) {
        errorList.eventDate = t('errors.invalidDate')
    }

    return {
        errorList,
        hasError: Object.keys(errorList).length > 0,
    }
}
```

(The `formData` parameter was untyped `any` before — typed it against what the function actually reads, without changing behavior.)

- [ ] **Step 3: Update the two callers**

In `src/features/CreateWishlist/model.ts`, add the import and hook call:

```typescript
import { useTranslations } from 'next-intl'
```

Inside `useCreateWishlistModel`, near the top of the hook body:

```typescript
const t = useTranslations('wishlistForm')
```

Change the call site:

```typescript
const {errorList, hasError} = validateWishlistForm(formData, t);
```

Apply the identical change to `src/features/UpdateWishlist/model.ts` (same import, same `const t = useTranslations('wishlistForm')`, same call-site update).

- [ ] **Step 4: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: at `/fr/wishlists`, open "Create New Wishlist", submit with an empty name — confirm the error reads in French; switch to `/en/wishlists`, repeat — confirm English.

- [ ] **Step 5: Commit**

```bash
git add src/entities/wishlist/lib/validateWishlistForm.ts src/features/CreateWishlist/model.ts src/features/UpdateWishlist/model.ts src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate wishlist form validation errors"
```

---

### Task 5: Translate wish form validation errors

**Files:**
- Modify: `src/features/AddWish/model.ts`
- Modify: `src/features/EditWish/model.ts`
- Modify: `src/shared/i18n/messages/en.json` (new namespace `wishForm.errors`)
- Modify: `src/shared/i18n/messages/fr.json` (new namespace `wishForm.errors`)

**Interfaces:**
- Consumes: nothing from earlier tasks (independent of Task 4 — different namespace, different files).

- [ ] **Step 1: Add the error-message keys**

Add to `en.json` under `wishForm`, new `errors` object:

```json
"errors": {
  "nameRequired": "Name is required",
  "descriptionRequired": "Description is required",
  "priceMustBePositive": "Price must be greater than 0",
  "invalidUrl": "Invalid URL",
  "invalidImageUrl": "Invalid image URL"
}
```

Add to `fr.json` under `wishForm`, same shape:

```json
"errors": {
  "nameRequired": "Le nom est requis",
  "descriptionRequired": "La description est requise",
  "priceMustBePositive": "Le prix doit être supérieur à 0",
  "invalidUrl": "URL invalide",
  "invalidImageUrl": "URL d'image invalide"
}
```

- [ ] **Step 2: Wire `AddWish/model.ts`**

Read lines 40-50 (the `validateForm` block) before editing to confirm exact current structure, then add `const t = useTranslations('wishForm')` inside the hook (import `useTranslations` from `next-intl`), and replace:

```typescript
if (!formData.name.trim()) newErrors.name = "Le nom est requis"
if (!formData.description.trim()) newErrors.description = "La description est requise"
// (price check line — keep condition, replace message)
newErrors.price = "Le prix doit être supérieur à 0"
if (formData.purchaseUrl && !isValidUrl(formData.purchaseUrl)) newErrors.purchaseUrl = "URL invalide"
if (formData.imageUrl && !isValidUrl(formData.imageUrl)) newErrors.imageUrl = "URL d'image invalide"
```

with:

```typescript
if (!formData.name.trim()) newErrors.name = t('errors.nameRequired')
if (!formData.description.trim()) newErrors.description = t('errors.descriptionRequired')
// (price check line — keep condition, replace message)
newErrors.price = t('errors.priceMustBePositive')
if (formData.purchaseUrl && !isValidUrl(formData.purchaseUrl)) newErrors.purchaseUrl = t('errors.invalidUrl')
if (formData.imageUrl && !isValidUrl(formData.imageUrl)) newErrors.imageUrl = t('errors.invalidImageUrl')
```

- [ ] **Step 3: Wire `EditWish/model.ts`**

Identical change at its own `validateForm` block (lines 59-64 per the pre-plan audit) — same five replacements, same `t('wishForm')` hook call added.

- [ ] **Step 4: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: at `/fr/wishlist/[id]` (as owner), open "Add Item", submit with price `0` — confirm French error; switch to `/en/...`, repeat — confirm English. Repeat for "Edit" on an existing item.

- [ ] **Step 5: Commit**

```bash
git add src/features/AddWish/model.ts src/features/EditWish/model.ts src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate wish form validation errors"
```

---

### Task 6: Translate the gift-pot modals (ContributeModal, CreatePotModal/Button)

**Files:**
- Modify: `src/features/ContributePot/ui/ContributeModal.tsx`
- Modify: `src/features/CreatePot/ui/CreatePotModal.tsx`
- Modify: `src/features/CreatePot/ui/CreatePotButton.tsx`
- Modify: `src/shared/i18n/messages/en.json` (namespace `contributeModal` — update stale keys, add new ones; new namespace `createPotModal`)
- Modify: `src/shared/i18n/messages/fr.json` (same)

**Interfaces:**
- Consumes: nothing from earlier tasks.

- [ ] **Step 1: Update the stale `contributeModal` keys and add new ones**

The existing `contributeModal` namespace in both JSON files still has the *pre-pledge-rewrite* copy (`"title": "Gift to the pot"`, `"sendGift": "Send gift"`, etc.) from before this session's honesty pass — it predates the "this is a pledge, not a payment" rewrite and doesn't match what's on screen. Replace the whole `contributeModal` object in `en.json` with:

```json
"contributeModal": {
  "title": "Pledge to the pot",
  "collectiveGift": "A collective gift for {ownerName}",
  "organisedBy": "Organised by {creatorName}",
  "alreadyPledged": "{currency}{amount} already pledged by friends",
  "beFirst": "Be the first to pledge to the pot",
  "youvePledged": "You've pledged {currency}{amount}",
  "loginPrompt": "Log in to contribute to this pot.",
  "logIn": "Log in",
  "contributionLabel": "Your contribution",
  "disclaimer": "This records a pledge — it isn't a payment. {creatorName} is collecting the money from everyone directly, so make sure you get it to them yourself.",
  "cancel": "Cancel",
  "adding": "Adding…",
  "addPledge": "Add pledge"
}
```

Replace the whole `contributeModal` object in `fr.json` with:

```json
"contributeModal": {
  "title": "Promettre au pot",
  "collectiveGift": "Un cadeau collectif pour {ownerName}",
  "organisedBy": "Organisé par {creatorName}",
  "alreadyPledged": "{currency}{amount} déjà promis par des amis",
  "beFirst": "Soyez le premier à contribuer au pot",
  "youvePledged": "Vous avez promis {currency}{amount}",
  "loginPrompt": "Connectez-vous pour contribuer à ce pot.",
  "logIn": "Se connecter",
  "contributionLabel": "Votre contribution",
  "disclaimer": "Ceci enregistre une promesse — pas un paiement. {creatorName} collecte l'argent directement auprès de chacun, alors assurez-vous de le lui remettre vous-même.",
  "cancel": "Annuler",
  "adding": "Ajout…",
  "addPledge": "Ajouter ma promesse"
}
```

Note this drops the old `alreadyPooled`/`yourContribution`/`sendGift` keys (renamed to `alreadyPledged`/`youvePledged`/`addPledge` to match current copy) and adds `organisedBy`, `loginPrompt`, `logIn`, `disclaimer` which had no keys at all.

- [ ] **Step 2: Wire `ContributeModal.tsx`**

Add `import { useTranslations } from 'next-intl'` and `const t = useTranslations('contributeModal')` inside the component. Replace per this table (all against `src/features/ContributePot/ui/ContributeModal.tsx`):

| Line | Current | Replace with |
|---|---|---|
| 36 | `Pledge to the pot` | `{t('title')}` |
| 52 | `A collective gift for <strong>{ownerName}</strong>` | `{t.rich('collectiveGift', { ownerName: () => <strong>{ownerName}</strong> })}` — if `t.rich` feels heavier than needed, simpler alternative: keep the JSX wrapper and just translate the surrounding text via two half-strings is fragile with ICU; prefer `t('collectiveGift', {ownerName})` and drop the bold styling, OR wrap the whole translated string in `<strong>`. Recommended: `<>{t('collectiveGiftPrefix')}</>` is overengineering — simplest correct fix is `t('collectiveGift', { ownerName })` rendered as plain text (drop the `<strong>`, since ICU interpolation renders as a string, not JSX) |
| 55 | `Organised by {creatorName}` | `{t('organisedBy', { creatorName })}` |
| 59 | `` `${currency}${totalContributed.toFixed(2)} already pledged by friends` `` | `t('alreadyPledged', { currency, amount: totalContributed.toFixed(2) })` |
| 63 | `Be the first to pledge to the pot` | `{t('beFirst')}` |
| 68 | `` You've pledged {currency}{userContributed.toFixed(2)} `` | `{t('youvePledged', { currency, amount: userContributed.toFixed(2) })}` |
| 77 | `Log in to contribute to this pot.` | `{t('loginPrompt')}` |
| 84 | `Log in` | `{t('logIn')}` |
| 91 | `Your contribution` | `{t('contributionLabel')}` |
| 110-111 | disclaimer paragraph | `{t('disclaimer', { creatorName })}` |
| 121 | `Cancel` | `{t('cancel')}` |
| 128 | `isSubmitting ? 'Adding…' : 'Add pledge'` | `isSubmitting ? t('adding') : t('addPledge')` |

For the line-52 bold-name case: simplest correct approach given ICU interpolation returns a plain string is to drop the `<strong>` wrapper and accept plain text, since introducing `t.rich` here is disproportionate to a two-word style choice. If the bold emphasis matters visually, use CSS (`font-weight` on a wrapping `<strong>` via `dangerouslySetInnerHTML` is **not** an option — no `dangerouslySetInnerHTML` exists anywhere in this codebase and this plan does not introduce one). Decide when implementing: either accept plain text, or keep `t.rich` with the tag function shown above (next-intl supports this natively — see next-intl docs for `t.rich`).

- [ ] **Step 3: Add `createPotModal` namespace**

Add to `en.json`:

```json
"createPotModal": {
  "triggerButton": "Start a gift pot",
  "title": "Start a gift pot",
  "loginRequired": "Log in to start a pot for {ownerName}. You'll also need to be invited by the wishlist owner.",
  "cancel": "Cancel",
  "logIn": "Log in",
  "inviteRequired": "Only guests invited by {ownerName} can start a pot. Ask them to share the wishlist with you via email.",
  "gotIt": "Got it",
  "confirmMessage": "You'll be the organizer of a collective gift pot for {ownerName}. Other guests can see that you started it and contribute anonymously.",
  "starting": "Starting…",
  "startThePot": "Start the pot"
}
```

Add to `fr.json`:

```json
"createPotModal": {
  "triggerButton": "Démarrer un pot commun",
  "title": "Démarrer un pot commun",
  "loginRequired": "Connectez-vous pour démarrer un pot pour {ownerName}. Vous devrez aussi avoir été invité par le propriétaire de la wishlist.",
  "cancel": "Annuler",
  "logIn": "Se connecter",
  "inviteRequired": "Seuls les invités de {ownerName} peuvent démarrer un pot. Demandez-leur de partager la wishlist avec vous par e-mail.",
  "gotIt": "Compris",
  "confirmMessage": "Vous serez l'organisateur d'un pot commun pour {ownerName}. Les autres invités verront que vous l'avez démarré et pourront contribuer anonymement.",
  "starting": "Démarrage…",
  "startThePot": "Démarrer le pot"
}
```

- [ ] **Step 4: Wire `CreatePotModal.tsx` and `CreatePotButton.tsx`**

In both files, add `import { useTranslations } from 'next-intl'` and `const t = useTranslations('createPotModal')`. In `CreatePotModal.tsx`, replace: all three `title="Start a gift pot"` props → `title={t('title')}`; the login-required message → `{t('loginRequired', { ownerName })}` (keep the `<strong>{ownerName}</strong>` wrapping only if you resolve it the same way as Task 6 Step 2's line-52 decision — for consistency, apply the same choice here); `Cancel` → `{t('cancel')}`; `Log in` → `{t('logIn')}`; the invite-required message → `{t('inviteRequired', { ownerName })}`; `Got it` → `{t('gotIt')}`; the confirm message → `{t('confirmMessage', { ownerName })}`; `isCreating ? 'Starting…' : 'Start the pot'` → `isCreating ? t('starting') : t('startThePot')`. In `CreatePotButton.tsx`, replace `Start a gift pot` → `{t('triggerButton')}`.

- [ ] **Step 5: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: as a non-owner guest on a public wishlist at `/fr/wishlist/[id]`, open "Contribute to the pot" (or "Start a gift pot" if no pot exists yet) and confirm every string is French, including the disclaimer; repeat at `/en/...`.

- [ ] **Step 6: Commit**

```bash
git add src/features/ContributePot/ui/ContributeModal.tsx src/features/CreatePot/ui/CreatePotModal.tsx src/features/CreatePot/ui/CreatePotButton.tsx src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate gift-pot modals, refresh stale contributeModal keys"
```

---

### Task 7: Translate the Comments feature

**Files:**
- Modify: `src/features/Comments/ui/CommentsSection.tsx`
- Modify: `src/views/wishlist/wishlist.tsx` (the two `<CommentsSection title=… emptyMessage=…>` call sites)
- Modify: `src/widgets/WishCard/WishCard.tsx` (the "💬 Comments" / "Hide comments" toggle)
- Modify: `src/shared/i18n/messages/en.json` (new namespace `comments`)
- Modify: `src/shared/i18n/messages/fr.json` (same)

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: `CommentsSection`'s `title`/`emptyMessage` props remain caller-supplied strings (unchanged prop shape) — callers now pass translated strings instead of English literals.

- [ ] **Step 1: Add the `comments` namespace**

Add to `en.json`:

```json
"comments": {
  "defaultTitle": "Comments",
  "defaultEmpty": "No comments yet — be the first to say something.",
  "loading": "Loading comments…",
  "placeholder": "Add a comment…",
  "posting": "Posting…",
  "postComment": "Post comment",
  "toggleShow": "Comments",
  "toggleHide": "Hide comments",
  "wishlistEmpty": "No comments yet — start the conversation about {ownerName}'s wishlist."
}
```

Add to `fr.json`:

```json
"comments": {
  "defaultTitle": "Commentaires",
  "defaultEmpty": "Aucun commentaire pour le moment — soyez le premier à en laisser un.",
  "loading": "Chargement des commentaires…",
  "placeholder": "Ajouter un commentaire…",
  "posting": "Envoi…",
  "postComment": "Publier",
  "toggleShow": "Commentaires",
  "toggleHide": "Masquer les commentaires",
  "wishlistEmpty": "Aucun commentaire pour le moment — lancez la conversation sur la wishlist de {ownerName}."
}
```

- [ ] **Step 2: Wire `CommentsSection.tsx`**

`CommentsSection` is used both for the wishlist-level thread (wishlist.tsx passes custom `title`/`emptyMessage`) and the per-wish thread (WishCard.tsx uses the component's own defaults). Change the default prop values from English literals to calls into the `comments` namespace — but default *prop values* can't call hooks at the type level, so move the defaulting inside the component body instead:

```tsx
'use client'

import { useTranslations } from 'next-intl'
import { useCommentsModel } from '../model'
import type { TCommentTarget } from '@/shared/api/comment'
import styles from './CommentsSection.module.css'

type TCommentsSectionProps = {
    target: TCommentTarget
    enabled: boolean
    autoLoad?: boolean
    title?: string
    emptyMessage?: string
}

function formatTimestamp(iso: string): string {
    return new Date(iso).toLocaleDateString(undefined, { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' })
}

export const CommentsSection = ({
    target,
    enabled,
    autoLoad = true,
    title,
    emptyMessage,
}: TCommentsSectionProps) => {
    const t = useTranslations('comments')
    const resolvedTitle = title ?? t('defaultTitle')
    const resolvedEmptyMessage = emptyMessage ?? t('defaultEmpty')

    const {
        comments,
        isLoading,
        loadError,
        text,
        setText,
        isSubmitting,
        submitError,
        handleSubmit,
    } = useCommentsModel({ target, enabled, autoLoad })

    if (!enabled) return null

    return (
        <div className={styles.comments}>
            <h4 className={styles.comments__title}>{resolvedTitle}</h4>

            {isLoading && <p className={styles.comments__status}>{t('loading')}</p>}
            {loadError && <p className={styles.comments__error}>{loadError}</p>}

            {!isLoading && !loadError && comments.length === 0 && (
                <p className={styles.comments__empty}>{resolvedEmptyMessage}</p>
            )}

            {comments.length > 0 && (
                <ul className={styles.comments__list}>
                    {comments.map((comment) => (
                        <li key={comment.id} className={styles.comments__item}>
                            <div className={styles.comments__itemHeader}>
                                <span className={styles.comments__author}>{comment.authorName}</span>
                                <span className={styles.comments__time}>{formatTimestamp(comment.createdAt)}</span>
                            </div>
                            <p className={styles.comments__text}>{comment.text}</p>
                        </li>
                    ))}
                </ul>
            )}

            <form
                className={styles.comments__form}
                onSubmit={(e) => {
                    e.preventDefault()
                    handleSubmit()
                }}
            >
                <textarea
                    className={styles.comments__input}
                    value={text}
                    onChange={(e) => setText(e.target.value)}
                    placeholder={t('placeholder')}
                    rows={2}
                    disabled={isSubmitting}
                />
                {submitError && <p className={styles.comments__error}>{submitError}</p>}
                <button
                    type="submit"
                    className={styles.comments__submit}
                    disabled={isSubmitting || !text.trim()}
                >
                    {isSubmitting ? t('posting') : t('postComment')}
                </button>
            </form>
        </div>
    )
}
```

(`loadError`/`submitError` come from the API and stay English per the Global Constraints note — server-side messages are out of scope.)

- [ ] **Step 3: Update the wishlist-level call site**

In `src/views/wishlist/wishlist.tsx`, find the `<CommentsSection target={{ type: 'wishlist', wishlistId: id }} enabled={!userIsOwner} title="Comments" emptyMessage={...} />` call. Add `const tComments = useTranslations('comments')` alongside the file's other `useTranslations` usage (or reuse an existing `t` if the file already has one scoped elsewhere — check for naming collisions first), then:

```tsx
<CommentsSection
  target={{ type: 'wishlist', wishlistId: id }}
  enabled={!userIsOwner}
  title={tComments('defaultTitle')}
  emptyMessage={tComments('wishlistEmpty', { ownerName })}
/>
```

Since `CommentsSection` now defaults its own title when no prop is passed (Step 2), passing `title={tComments('defaultTitle')}` here is redundant with the component's own default — simplify by dropping the `title` prop entirely at this call site and let the component's internal default apply. Only pass `emptyMessage` (the wishlist-specific one), since that's the one that differs from the component's default.

- [ ] **Step 4: Update `WishCard.tsx`'s toggle button**

Find the `💬 {commentsOpen ? 'Hide comments' : 'Comments'}` button. Add `useTranslations('comments')` (`const t = useTranslations('comments')`) and replace with:

```tsx
💬 {commentsOpen ? t('toggleHide') : t('toggleShow')}
```

- [ ] **Step 5: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: at `/fr/wishlist/[id]` as a non-owner guest, confirm the wishlist-level "Comments" heading, empty-state text, placeholder, and post button are French, and the per-wish "💬 Commentaires" / "Masquer les commentaires" toggle switches correctly; repeat at `/en/...`.

- [ ] **Step 6: Commit**

```bash
git add src/features/Comments/ui/CommentsSection.tsx src/views/wishlist/wishlist.tsx src/widgets/WishCard/WishCard.tsx src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate comments feature"
```

---

### Task 8: Translate the account management page (ManageAccount / ProfilePage)

**Files:**
- Modify: `src/views/profile/ui/ProfilePage.tsx`
- Modify: `src/shared/i18n/messages/en.json` (namespace `profile` — update stale key, add new ones)
- Modify: `src/shared/i18n/messages/fr.json` (same)

**Interfaces:**
- Consumes: nothing from earlier tasks.

- [ ] **Step 1: Update the one stale key and add the missing ones**

`profile.publicProfileDesc` in both JSON files still has the pre-honesty-pass copy ("Allow others to find your profile and public wishlists by name.") that was rewritten this session to not promise search. Update it, and add keys for everything the ManageAccount rebuild introduced that was never in the JSON: the loading/error states, the Google-account security copy, the "view public profile" link, and the whole delete-confirmation modal. Replace the `profile` object in `en.json` with:

```json
"profile": {
  "myAccount": "My Account",
  "anonymous": "Anonymous",
  "memberSince": "Member since {date}",
  "wishlists": "Wishlists",
  "wishes": "Wishes",
  "shared": "Shared",
  "loading": "Loading…",
  "loadError": "Could not load your profile",
  "accountInfoTitle": "Account Info",
  "accountInfoSubtitle": "Update your name and review your email address.",
  "saved": "Saved",
  "displayName": "Display Name",
  "namePlaceholder": "Your name",
  "emailAddress": "Email Address",
  "emailHint": "Email cannot be changed at this time.",
  "saveChanges": "Save changes",
  "saving": "Saving…",
  "securityTitle": "Security",
  "securitySubtitle": "Update your password to keep your account secure.",
  "securitySubtitleGoogle": "You signed in with Google — there is no password to update.",
  "updated": "Updated",
  "currentPassword": "Current Password",
  "newPassword": "New Password",
  "confirmPassword": "Confirm Password",
  "updatePassword": "Update password",
  "updatingPassword": "Updating…",
  "preferencesTitle": "Preferences",
  "preferencesSubtitle": "Control notifications and privacy settings.",
  "publicProfile": "Public profile",
  "publicProfileDesc": "Let anyone with the link view your profile page and public wishlists.",
  "viewPublicProfile": "View your public profile ↗",
  "dangerZoneTitle": "Danger Zone",
  "dangerZoneSubtitle": "These actions are irreversible. Proceed with care.",
  "exportTitle": "Export my data",
  "exportDesc": "Download a copy of all your wishlists and wishes as JSON.",
  "export": "Export",
  "exporting": "Exporting…",
  "deleteAccountTitle": "Delete account",
  "deleteAccountDesc": "Permanently delete your account and all associated data. This cannot be undone.",
  "deleteAccount": "Delete account",
  "deleteModalTitle": "Delete your account?",
  "deleteModalMessage": "This permanently deletes your account, every wishlist you own, and all of their wishes. This cannot be undone.",
  "deleteModalCancel": "Cancel",
  "deleteModalConfirm": "Yes, delete my account",
  "deleting": "Deleting…"
}
```

Replace the `profile` object in `fr.json` with:

```json
"profile": {
  "myAccount": "Mon compte",
  "anonymous": "Anonyme",
  "memberSince": "Membre depuis {date}",
  "wishlists": "Wishlists",
  "wishes": "Souhaits",
  "shared": "Partagées",
  "loading": "Chargement…",
  "loadError": "Impossible de charger votre profil",
  "accountInfoTitle": "Informations du compte",
  "accountInfoSubtitle": "Modifiez votre nom et consultez votre adresse e-mail.",
  "saved": "Enregistré",
  "displayName": "Nom affiché",
  "namePlaceholder": "Votre nom",
  "emailAddress": "Adresse e-mail",
  "emailHint": "L'e-mail ne peut pas être modifié pour le moment.",
  "saveChanges": "Enregistrer les modifications",
  "saving": "Enregistrement…",
  "securityTitle": "Sécurité",
  "securitySubtitle": "Modifiez votre mot de passe pour sécuriser votre compte.",
  "securitySubtitleGoogle": "Vous vous êtes connecté avec Google — il n'y a pas de mot de passe à modifier.",
  "updated": "Mis à jour",
  "currentPassword": "Mot de passe actuel",
  "newPassword": "Nouveau mot de passe",
  "confirmPassword": "Confirmer le mot de passe",
  "updatePassword": "Mettre à jour le mot de passe",
  "updatingPassword": "Mise à jour…",
  "preferencesTitle": "Préférences",
  "preferencesSubtitle": "Gérez les notifications et les paramètres de confidentialité.",
  "publicProfile": "Profil public",
  "publicProfileDesc": "Permettez à quiconque possède le lien de consulter votre page de profil et vos wishlists publiques.",
  "viewPublicProfile": "Voir votre profil public ↗",
  "dangerZoneTitle": "Zone dangereuse",
  "dangerZoneSubtitle": "Ces actions sont irréversibles. Agissez avec prudence.",
  "exportTitle": "Exporter mes données",
  "exportDesc": "Téléchargez une copie de toutes vos wishlists et souhaits au format JSON.",
  "export": "Exporter",
  "exporting": "Export…",
  "deleteAccountTitle": "Supprimer le compte",
  "deleteAccountDesc": "Supprime définitivement votre compte et toutes les données associées. Cette action est irréversible.",
  "deleteAccount": "Supprimer le compte",
  "deleteModalTitle": "Supprimer votre compte ?",
  "deleteModalMessage": "Cette action supprime définitivement votre compte, toutes les wishlists que vous possédez et tous leurs souhaits. Cette action est irréversible.",
  "deleteModalCancel": "Annuler",
  "deleteModalConfirm": "Oui, supprimer mon compte",
  "deleting": "Suppression…"
}
```

- [ ] **Step 2: Wire `ProfilePage.tsx`**

Read the current full file (it was last rewritten mid-session and has drifted from what's quoted in this plan's research) before editing. Add `import { useTranslations } from 'next-intl'` and, inside the component, `const t = useTranslations('profile')`. Then replace every literal with its matching key from Step 1's table — the mapping is 1:1 by meaning (e.g. the loading-state heading → `t('loading')`, the "Could not load your profile" heading → `t('loadError')`, every label/button in the Account Info / Security / Preferences / Danger Zone sections → the correspondingly-named key, the delete-confirmation `<Modal title=… >` and its body paragraph and two buttons → the four `deleteModal*` keys, and the conditional security subtitle → `profile.hasPassword ? t('securitySubtitle') : t('securitySubtitleGoogle')`).

One value needs a locale-aware date instead of a fixed English formatter: `formatMemberSince` currently calls `.toLocaleDateString('en-US', {...})` unconditionally. Change it to accept the active locale:

```tsx
import { useLocale, useTranslations } from 'next-intl'
```

```tsx
function formatMemberSince(iso: string, locale: string): string {
    return new Date(iso).toLocaleDateString(locale, { month: 'long', year: 'numeric' })
}
```

and at the call site, `const locale = useLocale()` then `formatMemberSince(profile.createdAt, locale)`. Then wrap the result with the `memberSince` key: `t('memberSince', { date: formatMemberSince(profile.createdAt, locale) })`.

- [ ] **Step 3: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: at `/fr/profile`, confirm every section heading/label/button is French, the "Member since" date renders in French date formatting (e.g. "août 2026" not "August 2026"), toggle "Public profile" on and confirm the "Voir votre profil public ↗" link appears, and open the delete-account confirmation modal to confirm its copy is French. Repeat at `/en/profile`. Also test a Google-only test account (or temporarily fake `hasPassword: false` in dev tools) to confirm the Security section shows the Google copy instead of the password form.

- [ ] **Step 4: Commit**

```bash
git add src/views/profile/ui/ProfilePage.tsx src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate account management page, refresh stale profile keys"
```

---

### Task 9: Translate the public profile page

**Files:**
- Modify: `src/views/publicProfile/ui/PublicProfilePage.tsx`
- Modify: `src/shared/i18n/messages/en.json` (new namespace `publicProfile`)
- Modify: `src/shared/i18n/messages/fr.json` (same)

**Interfaces:**
- Consumes: `profile.memberSince` from Task 8 (already exists regardless of whether Task 8 has landed, since it's a pre-existing key) — no hard dependency on Task 8 being done first, but doing Task 8 first means one less "does this key already say what I need" check.

- [ ] **Step 1: Add the `publicProfile` namespace**

Add to `en.json`:

```json
"publicProfile": {
  "publicWishlistsTitle": "Public Wishlists",
  "emptyMessage": "{name} hasn't shared any public wishlists yet."
}
```

Add to `fr.json`:

```json
"publicProfile": {
  "publicWishlistsTitle": "Wishlists publiques",
  "emptyMessage": "{name} n'a pas encore partagé de wishlist publique."
}
```

- [ ] **Step 2: Wire the component**

```tsx
'use client'

import { useLocale, useTranslations } from 'next-intl'
import { WishlistList } from '@/widgets'
import { TWishlistCard } from '@/widgets/WishlistCard'

import s from './publicProfile.module.css'

type TPublicProfilePageProps = {
    name: string
    createdAt: string
    wishlists: TWishlistCard[]
    currentUserId: string
}

function getInitials(name: string): string {
    if (!name) return '?'
    return name
        .split(' ')
        .map((part) => part[0])
        .slice(0, 2)
        .join('')
        .toUpperCase()
}

function formatMemberSince(iso: string, locale: string): string {
    return new Date(iso).toLocaleDateString(locale, { month: 'long', year: 'numeric' })
}

export default function PublicProfilePage({ name, createdAt, wishlists, currentUserId }: TPublicProfilePageProps) {
    const t = useTranslations('publicProfile')
    const tProfile = useTranslations('profile')
    const locale = useLocale()

    return (
        <main className={s.publicProfile}>
            <div className={s.hero}>
                <div className={s.hero__inner}>
                    <div className={s.avatar}>
                        <span className={s.avatar__monogram}>{getInitials(name)}</span>
                    </div>
                    <h1 className={s.hero__name}>{name}</h1>
                    <span className={s.hero__badge}>{tProfile('memberSince', { date: formatMemberSince(createdAt, locale) })}</span>
                </div>
            </div>

            <div className={s.body}>
                <WishlistList
                    wishlistCardList={wishlists}
                    currentUserId={currentUserId}
                    title={t('publicWishlistsTitle')}
                    emptyMessage={t('emptyMessage', { name })}
                    showCreateButton={false}
                />
            </div>
        </main>
    )
}
```

This reuses `profile.memberSince` (already exists) rather than duplicating it under `publicProfile`, since it's the identical string shape in both places.

- [ ] **Step 3: Verify**

Run: `npx tsc --noEmit -p .` — expect silent.
Manual: visit `/fr/u/[id]` for an account with public profile enabled and at least one public wishlist — confirm "Wishlists publiques" and the French member-since date; visit `/en/u/[id]` and confirm English. Visit a public profile with zero public wishlists and confirm the empty message interpolates the name correctly in both locales.

- [ ] **Step 4: Commit**

```bash
git add src/views/publicProfile/ui/PublicProfilePage.tsx src/shared/i18n/messages/en.json src/shared/i18n/messages/fr.json
git commit -m "fix(i18n): translate public profile page"
```

---

### Task 10: Repo-wide sweep for anything missed

**Files:** none known in advance — this task's job is to find what the audit and Tasks 1-9 didn't catch.

**Interfaces:** none.

- [ ] **Step 1: Grep for remaining hardcoded French**

```bash
grep -rlnP '[À-ÿ]{3,}' src --include=*.tsx --include=*.ts | grep -v messages/fr.json
```

This catches accented-character runs (a strong French signal) outside the translation files. Cross-reference each hit against `src/shared/i18n/messages/fr.json` — a hit is a real gap only if the string isn't already routed through `t()`.

- [ ] **Step 2: Grep for remaining hardcoded English UI copy in files that already import `useTranslations`**

```bash
grep -rl "useTranslations" src --include=*.tsx | xargs grep -lP '>[A-Z][a-z]+ [a-z]+.*<' 
```

This is a heuristic (component already translates *something* but might still have a leftover literal) — inspect each match manually rather than trusting the regex blindly.

- [ ] **Step 3: Fix whatever Steps 1-2 turn up**

For each real gap found, add the key to both `en.json` and `fr.json` and wire it, following the exact pattern established in Tasks 1-9 (read the file, find the literal, add/reuse a key, replace, verify both locales in the browser).

- [ ] **Step 4: Final full-app manual pass**

With the dev server running, click through every page and every modal at `/fr/...`: landing, features, wishlists list (both tabs, owner filter), history, wishlist detail (owner view and guest view, including the gift pot and comments), profile, public profile, and the login/signup modal. Confirm nothing reads in English. Then repeat at `/en/...` and confirm nothing reads in French.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix(i18n): sweep and fix remaining hardcoded strings"
```

---

## Self-Review Notes

- **Spec coverage:** every hardcoded-string source identified during the pre-plan audit (`WishlistForm`, `WishForm`, five modal titles, two validators, `ContributeModal`, `CreatePotModal`/`CreatePotButton`, `CommentsSection` + its two call sites, `ProfilePage`, `PublicProfilePage`) has a task. Task 10 exists specifically because the audit that produced this plan was targeted at the components discussed this session, not a full-repo line-by-line read — it's the safety net for anything outside that set (e.g. other pre-existing modals not touched this session that might have the same issue).
- **Placeholder scan:** every task shows the actual before/after strings or a concrete lookup table, not "translate the remaining strings." The two exceptions are intentional and flagged inline: `WishForm.tsx`'s exact literal locations (Task 2) and `EditWishModal.tsx`/`ProposeWishModal.tsx`'s exact current title text (Task 3) — both were never read in full during planning, and the task explicitly instructs reading the file first rather than guessing content that wasn't verified.
- **Type/key consistency:** key names are used identically between the JSON-authoring steps and the component-wiring steps within each task (e.g. `contributeModal.addPledge` in Task 6 Step 1 matches `t('addPledge')` in Task 6 Step 2's table). `validateWishlistForm`'s new `t` parameter type (`(key: string) => string`) matches how next-intl's `t` is actually called in every wiring task.
